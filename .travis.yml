language: ruby
cache: bundler
rvm:
  - 2.5.6
  - 2.6.3
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
addons:
  postgresql: "11"
before_script:
    - cat $TRAVIS_BUILD_DIR/postgres-setup | psql -U postgres
